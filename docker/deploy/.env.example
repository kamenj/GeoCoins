# Production Configuration
# Copy this file to .env and edit the values

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=geocoins

# Backend Configuration
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=CHANGE_THIS_PASSWORD
DB_NAME=geocoins
NODE_ENV=production
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY
PORT=3000
ALLOWED_ORIGINS=http://localhost:8081,https://yourdomain.com

# Frontend Configuration
BACKEND_HOST=backend
BACKEND_PORT=3000

# Cloudflare Tunnel (Optional)
# If you want public access, configure these paths:
# TUNNEL_CONFIG=/path/to/your/tunnel/config.yml
# TUNNEL_CREDENTIALS=/path/to/your/credentials.json
# TUNNEL_CERT=/path/to/your/cert.pem
